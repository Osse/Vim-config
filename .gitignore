# Vim swap files are ignored
.*.sw?

#Ignore all the bundle directories except Vundle.
bundle/*
!bundle/vundle
bundle/vundle/doc/tags

#Ignore pyc files in UltiSnips
ultisnips_rep/plugin/UltiSnips/*.pyc

#Ignore tags file because it is huge
tags/tags
